sap.ui.define([
    "./base/BaseController"
], function (Controller) {
    "use strict";

    return Controller.extend("ui5odatav2demo.controller.App", {
        onInit: function () {
            Controller.prototype.onInit.apply(this);
        }
    });
});
